三范式是在设计关系型数据库时，为减少数据冗余，避免数据异常（插入/更新/删除）而建立的规范规则；

**1NF（第一范式）：** 原子性，表中每个字段是不可再分割的最小数据单元

**2NF（第二范式）：** 消除部分依赖，基于第一范式，非主键值依赖主键值

**3NF（第三范式）：** 消除传递依赖，在第二范式基础上，非主键字段之间不能存在传递依赖

反三范式：为提升查询性能，适当违背三范式，引入数据冗余的设计策略，减少表连接，可以用于读多写少，因查询性能达到瓶颈的场景；