1. 客户端发送SYN包 (同步序列编号) 到服务器 (请求建立连接)；

2. 服务器收到SYN包后 (处理该连接请求)，向客户端发送SYN+ACK包作为应答；

3. 客户端收到应答后，向服务器发送ACK包确认 (连接成功)；

抬杠：为什么不是两次握手？
> 答：两次握手无法防止重复连接初始化，无法同步初始序列号。
> 
> ps：两次握手服务器无法确认客户端是否收到自己可以连接的响应，可能导致半连接状态（服务器等待无用请求，浪费资源）