# 常用方法代码

###### 编写自定义代码需要调用的日志类

```plain

   // 创建日志对象
    private H3.DataModel.BizObject CreateLogBo(H3.IEngine engine, string schemaCode, string schemaName)
    {
        H3.DataModel.BizObject logBo = new H3.DataModel.BizObject(this.Request.Engine, engine.BizObjectManager.GetPublishedSchema("D000772039600ed8f404baf9d9e269d2ef0d55d"), H3.Organization.User.SystemUserId);
        logBo.CreatedBy = this.Request.UserContext.UserId;
        logBo["F0000014"] = schemaCode;
        logBo["F0000009"] = schemaName;
        logBo["F0000002"] = DateTime.Now;

        return logBo;
    }

    // 写日志结果
    private void WriteLog(H3.DataModel.BizObject logBo, DateTime startTime, DateTime endTime)
    {
        TimeSpan ts = endTime - startTime;
        logBo["F0000003"] = new DateTime();
        logBo["F0000004"] = ts.TotalMinutes;
        logBo["F0000005"] = "执行成功";
        logBo.Status = H3.DataModel.BizObjectStatus.Effective;
        logBo.Create();
    }
      //当找到了对应的actionName时，回复给第三方请求一个 key为“result”值为“success” 的结果
                response.ReturnData.Add("result", "success");
                //当找到了对应的actionName时，回复给第三方请求一个 key为“message”值为空字符串 的结果
                response.ReturnData.Add("message", string.Empty);

                // string stringValue =   this.Request.GetValue<string>("para1",   "defaultValue");
                H3.IEngine engine = this.Request.Engine;
                H3.DataModel.BizObject logBo = CreateLogBo(engine, "D000772028fe68f517b44f3a36a795bed058037", "计财域权限变更用户记录表");
                System.DateTime startTime = System.DateTime.Now;
                //方法执行

                System.DateTime endTime = System.DateTime.Now;
                WriteLog(logBo, startTime, endTime);

               response.ReturnData.Add("data", "ok");

```

###### 第三方调用氚云自定义接口

```plain

public class MyApiController : H3.SmartForm.RestApiController
{
    //此构造方法必须有，但是方法体内不需要写任何代码
    public MyApiController(H3.SmartForm.RestApiRequest request) : base(request) { }
    //第三方请求本自定义接口时，会触发本事件
    protected override void OnInvoke(string actionName, H3.SmartForm.RestApiResponse response)
    {
        try
        {
            //注：H3.Organization.User.SystemUserId   为系统默认用户Id
            if (actionName == "CreateOneData")
            {
                //当找到了对应的actionName时，回复给第三方请求一个 key为“result”值为“success” 的结果
                response.ReturnData.Add("result", "success");
                //当找到了对应的actionName时，回复给第三方请求一个 key为“message”值为空字符串 的结果
                response.ReturnData.Add("message", string.Empty);
                // string stringValue =   this.Request.GetValue<string>("para1",   "defaultValue");
                response.ReturnData.Add("data", "ok");
            }
            else
            {
                response.ReturnData.Add("result", "error");
                response.ReturnData.Add("message", "无法处理actionName为“" + actionName + "”的请求！");
            }
        }
        catch (Exception ex)
        {
            response.ReturnData.Add("result", "error");
            response.ReturnData.Add("message", ex.Message);
        }

    }

}

```
