# gitHub

1、使用cmd

```plain

# 1. 先进入你的目标父目录
cd /d xxxx文件夹保存路径

# 2. 执行克隆命令，并指定子目录名称（可选，如果不指定，Git 默认会用仓库名创建文件夹）
git clone xxxgithub https 地址
```

2、遇到网络问题

```plain

设置代理
git config --global http.proxy http://127.0.0.1:7897
```
